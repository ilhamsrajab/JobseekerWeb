export default {
    SET_USER(state, data) {
      state.user = data
    }
    // SET_AUTHENTICATED(state, value) {
    //     state.authStatus = value
    // },

    // SET_USER(state, value) {
    //     state.user = value
    // },

    // SET_TOKEN(state, value) {
    //     state.token = value
    // }
  };